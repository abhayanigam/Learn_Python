print("Hello World")

# Single line Comment :
# we use the hash (#) symbol to start writing a comment.

# Multi Line Comment :
# use triple quotes, either ''' or """.
# Example :
"""This is also a
perfect example of
multi-line comments"""

# Docstrings appear right after the definition of a function, class, or a module.
# The docstrings are associated with the object as their __doc__ attribute.

# Multi-line statement
# multiple lines with the line continuation character (\). For example:
# or use ( ), brackets [ ], and braces { } to do the same.
a = 1 + 2 + 3 + \
    4 + 5 + 6 + \
    7 + 8 + 9

print(a)

# multiple statements in a single line using semicolons, as follows:

b = 2; c = 3
print(b,c)
